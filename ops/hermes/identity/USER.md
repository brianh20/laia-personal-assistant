User prefers Hermes to have its own dedicated email address/mailbox rather than access to the user's personal inbox, and prefers free/no-paid-email-service setups when possible.
§
User prefers email assistance via read-only Gmail API/OAuth and scheduled summaries, not email gateway behavior or auto-replies.
§
User prefers Hermes to rotate account passwords without disclosing the new password back in chat; store secrets internally instead.
§
User wants the dashboard to be authoritative, remotely sourced, directly editable, cross-device synced, and to include a nightly browsable work log; they prefer a lighter, denser layout with a compact useful header.
§
User does not want empty/no-op email reminders when there is nothing relevant to report.
§
User does not want machine-readable cron/status updates or raw JSON notifications; only send human-meaningful updates.
§
User's GitHub username is brianh20.
§
User does not want raw Python/tool output pasted into chat; prefer brief human-readable intent/result updates only.
§
User prefers high-level summaries before choosing an auth approach; avoid verbose explanations. User generally does not log into accounts on phone, so Google login is off the table for dashboard auth; GitHub login is acceptable.